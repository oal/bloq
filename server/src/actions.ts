import {ActionManager} from "../../shared/actions";

export class ServerActionManager extends ActionManager {
    // Doesn't actually do anything yet. We currently have no actions to process on the server.
}